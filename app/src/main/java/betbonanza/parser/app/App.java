/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package betbonanza.parser.app;

import betbonanza.parser.list.LinkedList;

import static betbonanza.parser.utilities.StringUtils.join;
import static betbonanza.parser.utilities.StringUtils.split;
import static betbonanza.parser.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
